{
  "name": "twilio-call-scheduler",
  "version": "1.0.0",
  "description": "Schedule delayed phone calls via Cloudflare Workers + Twilio",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
